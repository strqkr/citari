import{BadRequestException,Body,Controller,Get,Headers,HttpCode,Param,Post,Query}from"@nestjs/common";import{ApiOperation,ApiTags}from"@nestjs/swagger";import{parseInput}from"../domain-http.js";import{availabilityQuerySchema,publicBookingSchema,slugSchema}from"./public.schemas.js";import{PublicService}from"./public.service.js";
@ApiTags("public")@Controller("public/:slug")export class PublicController{constructor(private readonly service:PublicService){}@Get()tenant(@Param("slug")slug:string){return this.service.tenant(parseInput(slugSchema,slug));}@Get("services")services(@Param("slug")slug:string){return this.service.services(parseInput(slugSchema,slug));}@Get("availability")availability(@Param("slug")slug:string,@Query()query:unknown){return this.service.availability(parseInput(slugSchema,slug),parseInput(availabilityQuerySchema,query));}@Post("bookings")@HttpCode(201)@ApiOperation({summary:"Create an idempotent public booking"})create(@Param("slug")slug:string,@Headers("idempotency-key")key:string|undefined,@Body()body:unknown){if(!key||key.length<16||key.length>200)throw new BadRequestException("A 16-200 character Idempotency-Key header is required");return this.service.createBooking(parseInput(slugSchema,slug),parseInput(publicBookingSchema,body),key);}}
