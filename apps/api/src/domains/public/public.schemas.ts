import{z}from"zod";export const slugSchema=z.string().min(2).max(100).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/);export const availabilityQuerySchema=z.object({serviceId:z.uuid(),locationId:z.uuid(),from:z.coerce.date(),to:z.coerce.date()}).refine(v=>v.from<v.to,"from must precede to").refine(v=>v.to.getTime()-v.from.getTime()<=31*86400000,"Range cannot exceed 31 days");export const publicBookingSchema=z.object({serviceId:z.uuid(),locationId:z.uuid(),startAt:z.coerce.date(),customer:z.object({firstName:z.string().trim().min(1).max(100),lastName:z.string().trim().min(1).max(200),email:z.email().transform(v=>v.toLowerCase()).optional(),phone:z.string().trim().min(5).max(30).optional(),consent:z.literal(true),notes:z.string().trim().max(1000).optional()}).refine(v=>v.email??v.phone,"Email or phone is required"),customerNotes:z.string().trim().max(1000).optional()}).strict();
